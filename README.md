# Subdomain Color

Simple nginx server that renders a page with the color of the subdomain.

## Usage

Run

```yaml
docker compose up -d
```

Visit

<http://red.color.localhost:8080/>
<http://419fff.color.localhost:8080/>
